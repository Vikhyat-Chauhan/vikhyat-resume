---
interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---

<div>
  <div class="font-mono text-2xl font-semibold text-white sm:text-3xl">{value}</div>
  <div class="mt-1 text-xs uppercase tracking-wider text-zinc-500">{label}</div>
</div>
